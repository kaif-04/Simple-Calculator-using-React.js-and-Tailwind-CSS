import React from 'react'; // Import the React library
import Calculator from './Calculator'; // Import the 'Calculator' component from the './Calculator' file

function App() {
  return (
    <div className="App">
      <Calculator /> {/* Render the 'Calculator' component within the 'App' component */}
    </div>
  );
}

export default App; // Export the 'App' component for use in other parts of the application
